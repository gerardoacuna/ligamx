<h1>Users</h1>

<%= link_to 'New User', new_admin_user_path, class: 'button' %>

<table class="twelve">
  <thead>
    <tr>
      <th>Email</th>
      <th>SignIn Count</th>
      <th>Current Sign In</th>
      <th>Last Sign In</th>
      <th colspan="3"></th>
    </tr>
  </thead>

<% @users.each do |user| %>
  <tr>
    <td><%= user.email %></td>
    <td><%= user.sign_in_count %></td>
    <td><%= user.current_sign_in_at %></td>
    <td><%= user.current_sign_in_at %></td>
    <td><%= link_to 'Show', [:admin, user] %></td>
    <td><%= link_to 'Edit', edit_admin_team_path(user) %></td>
    <td><%= link_to 'Destroy', [:admin, user], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>

</table>
